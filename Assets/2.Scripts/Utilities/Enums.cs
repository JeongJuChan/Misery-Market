public enum Language
{
    Ko,
    En,
    Ja,
}

public enum BGMName
{
    None,
}

public enum SFXName
{
    None,
}

public enum MixerGroup
{
    Master,
    BGM,
}

public enum SceneType
{
    Title,
    BaseCamp,
    Subway,
}

public enum UIState
{
    Idle,
    Showing,
    Hiding,
}

public enum PanelId
{
    None,
    MainMenu,
    Settings,
    Inventory,
    Shop,
    Dialog,
    Loading,
}